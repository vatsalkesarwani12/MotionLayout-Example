<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.vatsal.kesarwani.motionlayout_example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionLayoutExample">
        <activity android:name=".Motion5Activity"></activity>
        <activity
            android:name=".Motion4Activity"
            android:theme="@style/Theme.MotionLayoutExample.NoActionBar" />
        <activity
            android:name=".Motion3Activity"
            android:theme="@style/Theme.MotionLayoutExample.NoActionBar" />
        <activity
            android:name=".Motion2Activity"
            android:theme="@style/Theme.MotionLayoutExample.NoActionBar" />
        <activity
            android:name=".Motion1Activity"
            android:theme="@style/Theme.MotionLayoutExample.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.MotionLayoutExample.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>